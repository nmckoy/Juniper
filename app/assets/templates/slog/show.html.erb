<div class="container page-start">
  <div class="container col-md-6">
    <div class="media">
      <div class="media-body" ng-controller="SlogEditController">
        <h3 class="media-heading">Slog 
          <small class="text-muted">
            Posted <span ng-bind="slog.created_at | date:'medium'"/>
          </small>
        </h3>
        <p ng-click="editDesc(slog.user.currentuser)" 
           ng-show="!editDescFlag" ng-bind="slog.description"></p>
        <div ng-show="editDescFlag">
          <form>
            <textarea rows="5" cols="60" ng-model="slog.description"
                      focus="editDescFlag"></textarea>
          </form>
        </div>
      </div>
    </div>
  </div>
  <div class="container col-md-6">
    <h3>map?</h3>
  </div>
</div>